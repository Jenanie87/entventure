class Enemy extends MovableObject {
    // properties

    constructor() {
        super();
        this.x = 100 + Math.floor(Math.random() * 400);
/*         this.loadImage(path); */
    }

    // functions
}